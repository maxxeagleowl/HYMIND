# Decision Log

- 2026-05-19: Phase 5 — Added FastAPI HTTP wrapper (`src/hymind/api/server.py`) so n8n can trigger the HYMIND agent via HTTP and receive the full Markdown report content in the JSON response. Used a plain `def` endpoint (not `async def`) so FastAPI automatically runs the blocking LangGraph pipeline in a thread-pool executor without stalling the event loop. Failures return HTTP 200 with `status: failed` so n8n can read the error message without triggering its own HTTP error handling. Optional `x-api-key` auth controlled by `HYMIND_API_KEY` env var — empty value disables auth for local development. The API layer wraps existing `run_research()` and `generate_report()` without duplicating agent logic.
- 2026-05-19: Phase 5 — Added `GET /health` liveness endpoint returning immediately so n8n (or ngrok) can confirm the server is up before a scheduled run fires.

- 2026-05-19: Phase renumbering — Swapped Phase 5 and Phase 6. Phase 5 is now Distribution Automation & PDF Reporting (n8n scheduling, Execute Command, Markdown-to-PDF, Gmail, Telegram, delivery logging, archive automation). Phase 6 is now Documentation, Demo & Project Finalization (README, architecture docs, AGENTS.md, deliverable validation, demo, presentation, submission). Rationale: the project can be submitted at Phase 5 completion; finalization wraps everything including distribution. All documentation, memory, planning, and architecture files updated to reflect the new numbering.

- 2026-05-19: Phase 4 — Added `src/hymind/reporting/validator.py` with two public functions: `validate_findings()` (removes missing-URL and duplicate-URL entries, flags missing titles and empty snippets with warnings) and `check_state_quality()` (pre-report quality assessment). The validator runs inside `generate_report()` before context building, so the LLM always receives clean deduplicated findings. Findings with missing titles or short snippets are kept (not removed) because they can still contribute context — only URL-absent and duplicate entries are discarded.
- 2026-05-19: Phase 4 — Applied `=== Node START/END ===` log markers to all nine LangGraph workflow nodes and the report generator entry/exit points. This gives clear operational visibility during demo execution and makes log scanning unambiguous — node boundaries are immediately readable.
- 2026-05-19: Phase 4 — Added 73 new reliability tests (test_reliability.py) covering: RSS per-feed timeout, connection error, HTTP error, malformed feed, empty feed, multi-feed partial failure; Serper rate limit, server error, empty query, zero results; NewsAPI rate limit, server error, application error, removed article filtering; workflow node exception isolation for all collection and crawl nodes; finalize_state edge cases (missing start_time, invalid format, all-zero counts); report generator degraded state (empty merged_results, zero crawl success); OpenAI timeout and rate limit propagation; multi-source degraded pipeline (all APIs down, RSS-only path).
- 2026-05-19: Phase 4 — Patch paths in reliability tests target `hymind.workflows.research_workflow.read_feeds` and `hymind.workflows.research_workflow.crawl_many` (not the source module paths) because both are imported directly into research_workflow.py. Patching at the source module after import has no effect on the already-bound name in the workflow module.

- 2026-05-18: Moved distribution automation out of Phase 4. Phase 4 is now reserved for reliability, testing, validation, retry logic, error handling, logging, schema validation, and production hardening. Phase 5 now owns n8n orchestration, Markdown-to-PDF conversion, Gmail delivery, optional Telegram alerts, delivery logging, and report archiving so the core LangGraph/Python pipeline stays focused on research and synthesis. Phase 6 owns Documentation, Demo, and Project Finalization.
- 2026-05-18: Reorganized the docs directory into architecture, roadmap, operations, and planning subfolders while keeping `project_state.md` and `decision_log.md` in the docs root to preserve operational status tracking and decision history.
- 2026-05-18: Moved the reporting standards document into `docs/operations/reporting_standards.md` and corrected the filename typo.
- 2026-05-18: Split the skills directory into governance and operational categories to separate system behavior rules from domain execution rules.
- 2026-05-18: Phase 2 — Added CollectorProtocol as a structural typing.Protocol (not ABC) so existing module-level search() functions satisfy the interface without subclassing. Kept the abstraction minimal: one __call__ signature, no required base class.
- 2026-05-18: Phase 2 — Kept source_type as "news" (not "newsapi") for NewsAPI results. The value "news" is semantically correct (it describes the content category, not the API vendor) and matches the vocabulary used by Serper's news search type. Changing it to "newsapi" would break two Phase 1 tests (test_news_api_source_type_is_news, test_all_three_schemas_identical) and introduce a vendor-specific identifier into what is intended as a content-type field. If a finer-grained source distinction is needed in future, a separate source field already captures the publisher name.
- 2026-05-18: Phase 2 — Added _strip_html to news_api._normalize() snippet field. NewsAPI description fields occasionally contain HTML entities. Stripping at normalization time (same as RSS reader) keeps the validation layer simple and prevents HTML from entering the pipeline.
- 2026-05-18: Phase 2 — JSON decode error in _call_newsapi now logs the error context before re-raising ValueError. Adds observability without changing the error propagation contract.
- 2026-05-18: Phase 3 — Use Pinecone as external vector database for RAG memory. HYMIND needs persistent semantic memory for historical hydrogen market intelligence, trend comparison, and better report synthesis. Pinecone gives managed vector search, metadata filtering, and a production-style retrieval layer. Tradeoff: adds external dependency, credentials, possible cost and rate limit concerns. The pipeline degrades gracefully when Pinecone is not configured — both RAG nodes emit a warning and continue without raising.
- 2026-05-18: Phase 3 — Use text-embedding-3-small as the default embedding model (configurable via OPENAI_EMBEDDING_MODEL). Chosen for cost efficiency (lowest cost OpenAI embedding model) and 1536-dimension output suitable for semantic search on short research snippets.
- 2026-05-18: Phase 3 — RAG nodes inserted after crawl_selected (not after merge_and_deduplicate) so that crawled full-page content is available for richer embedding signal when storing findings.
- 2026-05-18: Phase 3 — rag_context stored as list of plain dicts (dataclasses.asdict) in AgentState rather than dataclass instances. Keeps state serialization simple and consistent with existing pattern where all state fields are dicts or primitives.
- 2026-05-18: Phase 3 — retrieve_context() in retriever.py catches all exceptions and returns [] rather than propagating. This is intentional: RAG retrieval is a quality enhancement, not a hard requirement. A Pinecone outage must never block report generation.
